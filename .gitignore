/target
*.swp
*.swo
.DS_Store
tests/.test-env
.worktrees/
.claude/worktrees/
benchmarks/results/

# Vendored iceberg-rust has its own resolver; its Cargo.lock is not checked in.
vendor/iceberg-rust/Cargo.lock

# Secrets & certs
.env
.env.*
# Allowlist the documented template so `cp .env.example .env` works.
!.env.example
*.pem
*.key
*.crt
*.p12

# Logs
*.log
*.jsonl
log*.txt
