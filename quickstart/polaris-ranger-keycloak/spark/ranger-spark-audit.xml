<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!--
  Ranger Spark plugin audit configuration for the polaris-ranger-keycloak stack.

  Audit is disabled to keep the stack simple. For production, configure
  xasecure.audit.destination.solr or xasecure.audit.destination.elasticsearch.
-->
<configuration>

  <property>
    <name>xasecure.audit.is.enabled</name>
    <value>false</value>
  </property>

</configuration>
