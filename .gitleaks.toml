# gitleaks configuration for SQE.
#
# Extends the built-in default ruleset and allowlists the known false positives
# in this repo. gitleaks auto-discovers this file when it scans from the repo
# root, so it is honored by:
#   * local runs        -> gitleaks dir .        (or gitleaks detect)
#   * the CI gate        -> aikido-secret-scan job (gitleaks detect --no-git)
# without any --config flag.
#
# Design rule: keep suppressions as narrow as possible so real secrets are still
# caught. Prefer, in order:
#   1. an inline `gitleaks:allow` comment on the offending line (travels with the
#      code, cannot mask a leak in any other file) -- used for the test fixtures
#      in crates/sqe-core/src/secret.rs,
#   2. a path-scoped allowlist (condition = "AND": placeholder allowed ONLY in
#      the docs/scripts that use it),
#   3. an exact-value allowlist for a unique, unmistakable example string.
# Broad, unscoped regexes are avoided: a loose pattern can unmask a real secret
# that merely contains the same substring.
#
# NOTE: this scans committed content only. It does NOT catch runtime leaks such
# as a secret written to a log at execution time -- those need a code fix.

[extend]
useDefault = true

# 1. Generated build output and vendored third-party code. Never our secrets;
#    scanned upstream, not here. Also keeps the scan fast (34MB vs 620MB).
[[allowlists]]
description = "Build output and vendored code"
paths = [
    '''(^|/)target/''',
    '''(^|/)docs/target/''',
    '''(^|/)vendor/''',
]

# 2. Documentation and test-script credential placeholders. condition = "AND"
#    means a hit is allowed ONLY when it is both in one of these paths AND matches
#    a placeholder pattern -- the same string in engine source is still flagged.
[[allowlists]]
description = "Doc / test-script credential placeholders (path-scoped)"
condition = "AND"
paths = [
    '''(^|/)docs/''',
    '''(^|/)openspec/''',
    '''(^|/)testing/''',
    '''(^|/)scripts/''',
]
regexes = [
    '''your-password''',
    '''\bs3cr3t\b''',
    '''sqe_k?_?abc123def456''',
]

# 3. Unique example values matched by their exact content. Each is specific
#    enough that it cannot collide with a real secret.
[[allowlists]]
description = "Unique example values"
regexes = [
    # Example API key in api_key.rs doc-comment / configuration.md
    '''sqe_k?_?abc123def456''',
    # PKCS#1 RSA key used only for JWT-signing tests (bearer_token.rs
    # TEST_RSA_PRIVATE_KEY_PEM); matched by its unique leading bytes so a real
    # RSA key cannot be masked. Lives inside a string literal, so it cannot take
    # an inline gitleaks:allow comment.
    '''MIIEowIBAAKCAQEAvX0NLGQkeecTCMwoDPx6Qkutsf0PWBEjZnNm4vkmTTvHD1F0''',
    # Test fixtures in crates/sqe-core/src/secret.rs. These carry inline
    # `gitleaks:allow` comments in the working tree, but the history scan
    # (aikido-secret-scan-history) reads old blobs where the comment was not
    # present yet, so the inline form cannot suppress them. Both are unmistakable
    # dummies used to assert that Debug redaction works.
    '''hunter2-very-private''',
    '''0123456789abcdef''',
    # Documented example API key. The `abc123def456` variant is already listed
    # above; this is the second placeholder value used in the deployment docs
    # (historically docs/deployment.md, now under docs/site/).
    '''sqe_k?_?xyz789ghi012''',
    # HMAC-SHA256 fixture digest in sha256_udf.rs (openssl of key=pepper / msg=SSN).
    # Not a secret. rustfmt put the hex on its own line and generic-api-key
    # started matching it on every pipeline.
    '''40966d99be0fda85dac0b2f8d9f00b434b89b0d3e2b3f4bdcbf1dd31a2b7092f''',
]

# 4. History-only false positives that cannot be expressed as a value match.
#    The aikido-secret-scan-history job scans every commit, so a finding in a
#    blob that predates a rename or an inline `gitleaks:allow` stays flagged
#    forever and no working-tree edit can clear it.
#
#    These two test scripts build Trino-compat HTTP calls whose credentials come
#    from shell variables ($SQE_USER, $TRINO_PASSWORD, ...), which the curl-auth
#    rules match on shape alone. The allowlist is scoped three ways at once
#    (condition = "AND"): only these two files, only a line that actually
#    contains a `$`-interpolated value, and only via `regexTarget = "line"`.
#    A hardcoded literal such as `curl -u admin:s3cr3tpw` has no `$` on the line
#    and is still reported. Verified against a probe repo: a planted
#    non-placeholder AWS secret in scripts/distributed-test.sh is still caught.
[[allowlists]]
description = "Variable-interpolated curl auth in test scripts (history scan)"
condition = "AND"
paths = [
    '''(^|/)scripts/distributed-test\.sh$''',
    '''(^|/)scripts/verify_trino_compatibility\.sh$''',
]
regexTarget = "line"
regexes = [
    '''curl[^\n]*\$\{?[A-Za-z_][A-Za-z0-9_]*''',
]

# 5. Same history-only class, different value: the Ranger access-control plan
#    quotes runnable `curl -s -u admin:'rangerR0cks!'` recipes against the local
#    quickstart. `rangerR0cks!` is the quickstart's PUBLISHED default, already in
#    docker-compose.yml, ranger/install.properties and the quickstart README as a
#    `${RANGER_ADMIN_PASSWORD:-rangerR0cks!}` default. It gates nothing outside a
#    developer's laptop.
#
#    Why an allowlist and not an edit: `aikido-secret-scan-history` reads every
#    commit, so the finding lives in the blob at 288e5b6 forever and no
#    working-tree change can clear it. Rewriting main's history to remove three
#    doc lines would be far worse than the finding.
#
#    Scoped three ways (condition = "AND"): only this one file, only a line
#    carrying this exact known-public value, and via `regexTarget = "line"`. A
#    different credential in the same file is still reported, and this value in
#    any other path is still reported. Note the unquoted `-u admin:rangerR0cks!`
#    form in docs/internal/plans/2026-06-18-ranger-access-control-backend.md does
#    NOT trip `curl-auth-user` and needs no entry -- the rule matches the quoted
#    shape.
[[allowlists]]
description = "Quickstart default Ranger password in a plan doc (history scan)"
condition = "AND"
paths = [
    '''(^|/)docs/superpowers/plans/2026-07-31-ranger-access-control-e2e\.md$''',
]
regexTarget = "line"
regexes = [
    '''curl[^\n]*-u admin:'rangerR0cks!'''',
]

# 6. Block 5 again, one plan doc later. The Spark/Ranger parity plan carries the
#    same runnable Ranger recipes, in the safer defaulted form:
#
#      curl -s -u "admin:${RANGER_ADMIN_PASSWORD:-<the same published default>}"
#
#    `curl-auth-user` matches the SHAPE, so parameterising the password does not
#    help: the literal default still sits on the line. Same published quickstart
#    value as block 5, same reasoning, same three-way scoping, and the same reason
#    it has to be an allowlist rather than an edit -- the finding lives in the blob
#    at 037681b and the history scan reads every commit.
#
#    That this recurred one plan later says it will keep recurring: any doc quoting
#    a runnable Ranger curl trips it. The durable fix is a rule-level exception for
#    this one known-public value, which belongs upstream in the aikido policy
#    rather than in a seventh per-file block here. This keeps main publishing in
#    the meantime.
[[allowlists]]
description = "Quickstart default Ranger password in the Spark parity plan (history scan)"
condition = "AND"
paths = [
    '''(^|/)docs/superpowers/plans/2026-08-06-spark-ranger-access-control-parity\.md$''',
]
regexTarget = "line"
regexes = [
    '''curl[^\n]*-u "admin:\$\{RANGER_ADMIN_PASSWORD:-rangerR0cks!\}"''',
]
