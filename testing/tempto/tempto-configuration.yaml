databases:
  trino:
    host: tls-proxy
    port: 8443
    server_address: https://tls-proxy:8443
    jdbc_driver_class: io.trino.jdbc.TrinoDriver
    jdbc_url: jdbc:trino://tls-proxy:8443/iceberg/default?SSL=true&SSLVerification=NONE
    jdbc_user: root
    jdbc_password: s3cr3t
    jdbc_pooling: false

tests:
  assert:
    float_tolerance: 0.000001

# Unused, but the product-tests jar registers an LDAP SuiteModuleProvider
# unconditionally; these keys must exist for the Guice injector to build.
# No LDAP test is on the allow-list, so nothing ever connects here.
ldap:
  admin:
    dn: cn=admin,dc=trino,dc=testldap,dc=com
    password: admin
  url: ldap://ldapserver
