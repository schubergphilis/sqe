# Curated upstream Iceberg product-tests to run against SQE (no Spark/Hive/HDFS).
# Format: one tempto test matcher per line; '#' comments and blank lines ignored.
# Matchers are substring-matched against test names by the tempto runner.
#
# STATUS (2026-06-29): all upstream Iceberg tests are currently BLOCKED against
# SQE by a response-shape bug -- SQE emits `data: []` for column-less DDL/update
# statements, which the Trino 465 JDBC client rejects ("Columns must be set when
# decoding data"). See testing/tempto/exclusions.md "Headline finding". Once that
# is fixed, expand this list and triage per-method.
#
# These are the closest-to-pure-onTrino Iceberg classes at trino tag 465. Each
# still has caveats (see exclusions.md): partition-evolution uses one onSpark
# setup call, concurrent-insert is tagged hms_only.
io.trino.tests.product.iceberg.TestIcebergPartitionEvolution
io.trino.tests.product.iceberg.TestIcebergInsert
