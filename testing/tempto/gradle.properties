# The trino-product-tests dependency graph is large; give Gradle enough heap
# to resolve it inside the container (default heap OOMs during resolution).
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=768m
org.gradle.daemon=false
